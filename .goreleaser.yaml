version: 2

project_name: quark

builds:
  - id: runtime
    dir: runtime
    main: ./cmd/runtime
    binary: runtime
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64

  - id: quark
    dir: cli
    main: ./cmd/quark
    binary: quark
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X github.com/quarkloop/cli/pkg/buildinfo.Version={{.Version}}

  - id: bash
    dir: plugins/tools/bash
    main: ./cmd/bash
    binary: bash
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64

  - id: fs
    dir: plugins/tools/fs
    main: ./cmd/fs
    binary: fs
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64

  - id: web-search
    dir: plugins/tools/web-search
    main: ./cmd/web-search
    binary: web-search
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64

archives:
  - format: tar.gz
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    format_overrides:
      - goos: windows
        format: zip

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^chore:"
      - "^ci:"
      - "^test:"
      - "^refactor:"
