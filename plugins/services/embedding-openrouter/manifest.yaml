name: embedding-openrouter
version: "1.0.0"
type: service
mode: api
description: "OpenRouter text embeddings over gRPC"
author: quarkloop
license: Apache-2.0

service:
  address_env: QUARK_EMBEDDING_ADDR
  skill: SKILL.md
  proto_services:
    - quark.embedding.v1.EmbeddingService
