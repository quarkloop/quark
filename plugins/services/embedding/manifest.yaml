name: embedding
version: "1.0.0"
type: service
mode: api
description: "Local deterministic text embeddings over gRPC"
author: quarkloop
license: Apache-2.0

service:
  address_env: QUARK_EMBEDDING_ADDR
  skill: SKILL.md
  proto_services:
    - quark.embedding.v1.EmbeddingService
